import { SwitchClientPage } from './app.po';

describe('switch-client App', function() {
  let page: SwitchClientPage;

  beforeEach(() => {
    page = new SwitchClientPage();
  });
/*
  it('should display message saying app works', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('app works!');
  });
  */
});
